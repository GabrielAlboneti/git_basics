from test2 import nome


def testing_linter(x, y):
   return x + y


print(nome)

# git status: check the status of the current changes;
# git add .: adds all changes to the commit;
# git --no-pager: print the output normally in the terminal, without the program;
# git log --oneline: prints a summed up version of the git history
# git remote add/rm name: Add/removes a remote repository;
# git push name branch -u: makes the repository the standard repository;
#     - can push with only git push
# ssh-keygen: generates ssh keys to use with github
# git config: used to configures things like username and email;
